# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — never commit real secrets.

# Example entries (delete or replace as needed):
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_APPLICATION_CREDENTIALS=credentials.json
